<%= form_with(model: alarm, local: true) do |form| %>
  <% if alarm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alarm.errors.count, "error") %> prohibited this alarm from being saved:</h2>

      <ul>
      <% alarm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :repeat_time %>
    <%= form.time_select :repeat_time, id: :alarm_repeat_time %>
  </div>

  <div class="field">
    <%= form.label :repeat_date %>
    <%= form.date_select :repeat_date, id: :alarm_repeat_date %>
  </div>

  <div class="field">
    <%= form.label :repeat_once %>
    <%= form.check_box :repeat_once, id: :alarm_repeat_once %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
